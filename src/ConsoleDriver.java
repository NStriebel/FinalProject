public class ConsoleDriver {
    public static void main(String[] args) {
        //bulk of logic elsewhere
        //this should only have I/O logic
        //meaning talk to user, get user input, show changes to user

        //eg
        //create an object of type Game and call its methods
        //Game g = new Game();
        //while(g.notDone(){
        //  ask user for input
        //  g.displayStatus()
        //}

        //this lets you add a gui with minimal changes to game logic if any

        //the design needs main components, but not necessarily every class.
    }
}
